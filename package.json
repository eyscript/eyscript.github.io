{
  "name": "proyecto_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://eyscript.github.io/project_gamev1/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html -p 8000",
  "build": "parcel build index.html --out-dir dist --public-url /<repo-name>/",
  "predeploy": "npm run build",
  "deploy": "gh-pages -b master -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "phaser": "^3.24.1"
  }
}
